-c constraints.in
-c base.txt
black>=22.3.0
coverage
# NOTE(mrobinson) - Pinning click due to a unicode issue in black
# can remove after black drops support for Python 3.6
# ref: https://github.com/psf/black/issues/2964
click>=8.1
# NOTE(alan) - Added to cover the fact that is isn't specified in
# starlette even though it's required for TestClient
httpx
flake8
flake8-docstrings
mypy
pytest-cov
pytest-mock
pdf2image>=1.16.2
huggingface_hub>=0.11.1
ruff
types-pyyaml
